problem:
  class: NoisyPoint
  init_args:
    noise_std: 0.1

network:
  class: MLPFlow
  init_args:
    hidden_dim: 512
    num_blocks: 8

batch_size: 256
learning_rate: 1.0e-4
weight_decay: 0.0
total_steps: 10000
log_interval: 100
warmup_frac: 0.5
seed: 0
